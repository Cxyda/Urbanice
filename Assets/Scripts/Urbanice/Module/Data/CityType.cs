namespace Urbanice.Module.Data
{
    /// <summary>
    /// Enum to define the city size, integer value of the enum defines the max amount of city districts
    /// </summary>
    public enum CityType
    {
        Hamlet = 1,
        Village = 3,
        Town = 6,
        City = 12,
        Metropolis = 20
    }
}