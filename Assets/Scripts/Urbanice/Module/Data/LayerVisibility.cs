namespace Urbanice.Module.Data
{
    /// <summary>
    ///  Simple enum to draw / hide certain layers
    /// </summary>
    public enum LayerVisibility
    {
        Generate = 0,
        Skip = 1,
    }
}